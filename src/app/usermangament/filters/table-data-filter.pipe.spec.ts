import { TableDataFilterPipe } from './table-data-filter.pipe';

describe('TableDataFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TableDataFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
