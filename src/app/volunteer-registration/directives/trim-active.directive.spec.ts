import { TrimActiveDirective } from './trim-active.directive';

describe('TrimActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimActiveDirective();
    expect(directive).toBeTruthy();
  });
});
